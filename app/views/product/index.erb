<h2>Ассортимент товаров</h2>
  <% @product.each do |product| %>
    <div class="list-group">
      <h3><%= product.title %></h3>
      <p><%= product.description %></p>
      <p><%= product.price %></p>
      <p><%= product.rating %></p>
      <p><%= product.votes %></p>
    </div>
  <% end %>
